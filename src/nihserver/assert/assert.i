%ifndef NIHSERVER_ASSERT_ASSERT_I
%define NIHSERVER_ASSERT_ASSERT_I

%ifndef NIHSERVER_ASSERT_ASSERT_S

; void assert(bool cond, const char *msg);
extern assert

; bool assert_true(bool cond, const char *msg);
extern assert_true

%endif

%endif
